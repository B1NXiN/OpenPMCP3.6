package pmcp.mode.move;

import pmcp.mode.Mode;
import pmcp.utils.category.Category;

public class GuiMove extends Mode {
    public GuiMove() {
        super("GuiMove", "窗口移动", "允许你在打开窗口的时候还能移动", Category.MOVE);
    }
}
